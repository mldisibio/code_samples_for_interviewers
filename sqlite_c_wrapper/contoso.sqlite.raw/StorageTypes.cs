/*
using System;

namespace contoso.sqlite.raw
{
    /// <summary>Defines the fundamental data type for each value stored in a sqlite database as returned by sqlite3_column_type().</summary>
    public enum StorageType : int
    {
        /// <summary>The value is a signed integer, stored in 1, 2, 3, 4, 6, or 8 bytes depending on the magnitude of the value.</summary>
        Integer = 1,
        /// <summary>The value is a floating point value, stored as an 8-byte IEEE floating point number.</summary>
        Float = 2,
        /// <summary>The value is a text string, stored using the database encoding (UTF-8, UTF-16BE or UTF-16LE).</summary>
        Text = 3,
        /// <summary>The value is a blob of data, stored exactly as it was input.</summary>
        Blob = 4,
        /// <summary>The value is a NULL value.</summary>
        Null = 5
    }
}
*/
